apply plugin: 'war'

war { archiveName 'pcw-weblayer.war' }

dependencies {
	providedCompile project(':pcw-ejbservice')
	providedCompile group: 'javax.servlet', name: 'javax.servlet-api', version: servletapiVersion
}