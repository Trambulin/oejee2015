apply plugin: 'war'

war { archiveName 'weblayer.war' }

dependencies {
	providedCompile project(':ejbservice')
	providedCompile group: 'javax.servlet', name: 'javax.servlet-api', version: servletapiVersion
}