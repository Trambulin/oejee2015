apply plugin: 'war'

war { archiveName 'jcs-weblayer.war' }

dependencies {
	providedCompile project(':jcs-ejbservice')
	providedCompile group: 'javax.servlet', name: 'javax.servlet-api', version: servletapiVersion
}